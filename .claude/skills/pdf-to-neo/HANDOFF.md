# 引き継ぎ — 工場見積 PDF から NEO を作る仕事

別のセッション・別の担当者が、この一式を初めて触るときに最初に読む文書。
**最終更新: 2026-09-12**（実機コグニでの検証と Codex 監査を反映）

---

## 1. 何をする仕事か

自動車の修理工場から届いた**見積書 PDF** を読み取り、損害保険会社に出す**見積ファイル `.neo`** を作る。
`.neo` は自動車損害査定ソフト「**コグニセブン**」（Audatex Auda7）が読み書きする形式で、
中身は独自圧縮のコンテナに SQLite が入っている。

**求められる品質は「人がコグニセブンに手入力して作ったのと見分けがつかないこと」**。
金額が合うだけでは足りない。行の並び、手入力の印、連動の扱いまで実機と同じである必要がある。
コグニは**ファイルを開いて画面を切り替えた時点で中身を作り直す**ので、
「ファイルの中では合っているのに、コグニで開くと変わる」書き方をすると事故になる（§5 を必ず読む）。

---

## 2. まず読む順番

| 順 | 文書 | 何が書いてあるか |
|---|---|---|
| 1 | この HANDOFF.md | 全体の地図・絶対ルール・実機で確かめた事実 |
| 2 | `SKILL.md` | 作業手順（全 9 段）。実作業はこれに沿って進める |
| 3 | `reference/judgment_rules.md` | **判断規則の正本**。迷ったら必ずここ。他の文書と食い違ったらこれが正しい |
| 4 | `reference/reading_schema.md` | 見積書を印字どおり写す JSON（`reading.json`）の形 |
| 5 | `reference/estimate_schema.md` | 生成器が読む JSON（`estimate.json`）の形 |
| 6 | `reference/format_catalog.md` | 届く見積書式の分類と読み取り規則 |
| 7 | `reference/checklist.md` | 納品前の確認事項 |
| 8 | `../../../NEO_FILE_SPEC_COMPLETE.md` | NEO コンテナと各テーブルの仕様（解析結果） |
| 9 | `../../../ADDATA_REVERSE_LOOKUP_SPEC.md` | ADDATA（コグニのマスタ）の解析結果 |
| 10 | `../../../claude_neo_pipeline/README.md` | 生成器の設計方針・既知の制限・検証の回し方 |

**同じ話題が複数の文書にあるときは `judgment_rules.md` が正本**。
ここに無い新しい判断をしたら、作業のあとで必ず judgment_rules に書き足す（この一式は使うたびに育てる）。

---

## 3. 絶対に守ること

1. **合計を合わせるために明細の金額・指数を動かさない**。
   工場が出した数字がすべて。調整してよいのは「塗装の材料代」だけで、それも工場見積に材料代の内訳が無いときに限る。
   例外は損害保険会社が協定で「この行の指数をこうする」と指定したときだけ
2. **合計が合わないまま「できました」と言わない**。差額の原因が説明できないなら未完了として報告する
3. **工場の指数がコグニの標準値と違っても、工場の値をそのまま残す**（手入力の印を付けて保持する）
4. **顧客情報を外へ出さない**。検証物は必ずリポジトリ外の `%USERPROFILE%\Documents\NEO_check\<案件名>\` に置く。
   `*.neo` と `NEO_check` は git に入れない。Codex など外部サービスに送るプロンプトに顧客情報を含めない
5. **既存の NEO や案件フォルダのファイルを上書き・削除しない**。
   コグニ実機で確認するときも、すでに開かれている窓は触らない・閉じない

---

## 4. 作業の流れ（要約）

詳細は `SKILL.md`。1 コマンドで通す場合:

```
python .claude\skills\pdf-to-neo\scripts\make_neo.py "$env:USERPROFILE\Documents\NEO_check\<案件>" --name "<顧客>_<車名>"
```

（**PowerShell の書き方**。この環境の既定シェルは PowerShell で、`%USERPROFILE%` は展開されずに
そのまま Python へ渡り「フォルダが無い」で止まる。bash なら `$USERPROFILE`、
cmd なら `%USERPROFILE%`。迷ったら**絶対パスをそのまま書く**のが確実）

| 段 | すること | 見る文書 |
|---|---|---|
| 1 | 入力（工場見積 PDF・車検証）を揃える | SKILL.md §1 |
| 2 | PDF を読み、書式を分類する | format_catalog.md |
| 3 | ADDATA から車種を特定する | SKILL.md §3 |
| 4 | `reading.json` に**印字どおり**写す（判断はしない） | reading_schema.md |
| 5 | `make_neo.py` で 下書き → 突合せ → 生成 → 検算 | SKILL.md §5 |
| 6 | `_draft_notes` と ★ を判断規則で 1 つずつ解決し、検算が全部 OK になるまで繰り返す | judgment_rules.md |
| 6-2 | **協定のとき**: 工場見積どおりで合格させてから、`agree_calc.py` で候補を出し、損保の指示の方法（レート／指定行の工賃／骨格→鈑金／塗装一式／材料代）で協定額に合わせる。reading に `target_total` があると NEO の合計がその額でなければ不合格 | SKILL.md §6-2・判断規則 10-14 |
| 7 | コグニ実機で開いて確認する（**この文書の §5-1 の理由で重要**） | checklist.md |
| 8 | 納品する（NEO と**確認箇所シート** `<name>_claude_確認箇所.xlsx`（openpyxl が無い PC では `.csv`）を一緒に。人向けのメモは NEO の明細コメントに書かない） | SKILL.md §8・判断規則 10-22 |
| 9 | 日誌と判断規則に記録する | SKILL.md §9 |

**段 4 で判断をしないこと**が品質の鍵。ref の決定・左右分割・板金ランク・装備・塗装行の解釈・
費用の分類・レバーレートは、すべて段 5 のスクリプトが決める。人が先回りすると再現性が落ちる。

### 最初の 1 件を始めるには

1. **実行する場所**: リポジトリ root（`…\【自動集計システム※作成中】\files`）。
   スクリプトのパスはすべてここからの相対で書く
**フォルダが 2 つ出てくるので呼び分ける**:

- **元案件フォルダ**（共有ドライブ。入力が置いてある。この PC では
  `Z:\2026年\MM月\DD日\<損保>_<顧客>_<番号>_<車名>_<地域>\` の形。
  ドライブレターと構成は PC・担当者で違うので、分からなければパスを直接もらう）
- **作業フォルダ**（ローカル。自分で作る。`$env:USERPROFILE\Documents\NEO_check\<案件名>\`）

2. **作業フォルダを作る**: `$env:USERPROFILE\Documents\NEO_check\<案件名>\`。
   顧客情報を含むのでリポジトリの中には作らない。ここに `reading.json` を書き、
   生成物（`estimate.json` / `<name>.neo` / `<name>_確認箇所.xlsx`（または `.csv`）/ `report.md` / `inspect.json` / `reading_check.json`）もここに出る
3. **入力（工場見積 PDF・車検証）は元案件フォルダから探す**。Read ツールで開く（画像 PDF でも読める）。
   `pypdf` の文字層は FAX だと空なので当てにしない
4. **元案件フォルダに既存の NEO（立会で作ったもの）があれば必ず見る** —— 作業フォルダではなく
   **共有ドライブ側**にある。`python claude_neo_pipeline\tests\neo_diff.py <生成.neo> <既存.neo>` で
   全列を突き合わせると、合計・名称・作業区分の入れ方が確認できる
   （既存ファイルは**開くだけ**。上書き・削除は禁止）

---

## 5. 実機で確かめた事実（間違えると事故る）

**この節が引き継ぎの核心**。いずれも実機コグニまたは実機 NEO で裏を取ったもので、
推測ではない。日付は確認した日。

### 5-1. コグニは画面を開いた時点で中身を作り直す

塗装ページを開くと、コグニは**連動パネルを明細（ERParts）から作り直す**。
したがって明細に対応する部品が無い連動行は、**開いた瞬間に消える**。

**塗装パネル（PaintingPanel）の `AddedFrom` の決め方**（**塗装パネルを持つ**実機 NEO 31 本・塗装パネル 103 行で例外なし。内訳は連動 80 行・パネル追加 23 行）:

**`AddedFrom` と工賃印（`WageByManual`）は別々に決まる**。まず `AddedFrom`:

| 明細に**同じ部品コードかつ同じ修理方法**の行が | `AddedFrom` |
|---|---|
| ある | 0（W/S 連動） |
| 無い | 1（パネル追加） |

工賃印は**指数が標準どおりかどうか**が先に効く:

| 塗装パネルの指数 | `WageByManual` | `Manual` |
|---|---|---|
| 標準と違う（工場の独自指数） | **`#`**（`AddedFrom` が 0 でも 1 でも `#`） | 1 |
| 標準どおり・明細に対応行あり | `''` | 0 |
| 標準どおり・明細に対応行なし | `*` | 0 |

- **部品コードだけでなく修理方法（取替 / 修理 / 板金）まで一致**していること。実機 69 行で食い違い 0
- **指数が標準と違うこと（手入力）は `AddedFrom` と無関係**。
  手入力でも「明細にあれば `AddedFrom=0`」「無ければ 1」は変わらない（実装 `estimate_to_neo.py` の
  `'AddedFrom': 0 if (is_bankin or linked) else 1` と `'WageByManual': '#' if pnl_manual else …`）
- 実測（2026-09-11、W90 ハイエース TRH229・ボディ 20。明細の部品コードは 4800、塗装パネルは枝番の 4801）:

  | 4801 の書き方 | 塗装ページを開いた結果 | 塗装計 |
  |---|---|---|
  | `AddedFrom=1`・`*`（パネル追加） | 2 行とも残る | **176,560**（工場見積と一致） |
  | `AddedFrom=0`・`''`（連動） | **4801 が消え**、枚数 1 で再計算 | 142,610（33,950 不足） |

- 生成器はこの違反を `ValueError` で止める（`estimate_to_neo.py` の `_linked_ng`）

**同じ型の事故が他のテーブルでも起こりうる**。新しいテーブルに書き込む処理を足すときは、
「コグニがその画面を開いたとき、この行を作り直さないか」を必ず考える。

### 5-2. 標準値と違う値は「手入力」の印を付けないと戻される

**塗装パネル**（`PaintingPanel`）は `Manual=0` / `WageByManual=''` のままだと、
コグニが塗装ページ表示時に ADDATA の標準値へ再計算する（`Manual` 列があるのはこの表だけ）。
工場独自の指数は `Manual=1`・`WageByManual='#'` で保持する。

**明細**（`ERParts`）も同じ。`WageByManual=''` の行は装備条件から標準区分を選び直され、
標準に無い指数の行は `Time/Wage=-1` に消える。
指数の列が無い非コグニ書式の見積は、**reading.json に `"index_policy": "manual"`** と書いて
全指数を `#` にする（draft が estimate.json へ引き継ぐ）。

### 5-3. 「標準値」の欄には標準値を入れる

`PaintingPlan.BaseTimeStandard` / `BoothTimeStandard` / `BaseWageStandard*` には
**ADDATA の標準値**を入れる（見積値ではない）。見積値を標準として書くと、
工場が標準と違う加算基礎を出したときに「標準どおり」の顔をしてしまい、コグニの再計算で戻る。
塗装パネルの `Time`（見積値）と `TimeStandardNew/1/2/3`（標準値）と同じ流儀。

2026-09-12 にこれを直したところ、実機 NEO との総当たり比較で**差のある実験が 1/67 → 0/67 になった**。

### 5-4. 20.DB（塗装パネル）はボディごとに別の行・別のコードを持つ

- レコード 39 バイト。**6 バイト目 = ボディコード**（0 から数えると `[5]`。実装は `r[5]`。`ADDATA_REVERSE_LOOKUP_SPEC.md` のバイト表は 0 起点なので「| 5 |」と書いてある）。値は 0 = 全ボディ共通 / 10 / 20 …
- 同じパネルが**ボディ違いで別コード（枝番）**になる車がある（W90 ハイエース: 4800 / 4801）
- 面積（= 塗装指数）がボディで違うので、**必ず車のボディコードで選ぶ**
- ただし選んだ結果、明細の部品コードと塗装パネルのコードがずれる。そのパネルは §5-1 により
  「パネル追加」（`AddedFrom=1`・`*`）で書く
- **面積（＝塗装指数）が一意に決まらない組が実在する**（20.DB を持つ 1,210 車種を全走査。ADDATA 2026/09、2026-09-13）:
  枝番でコードも面積も違う組が **20 組（13 車種）**、同じコードに面積の違う行がある組が **258 組**。
  後者のほうが多く、2026-09-12 まで黙って先頭が選ばれていた（例 C39 ﾌ-ﾄﾞ 0600 = 131 / 149 d㎡）。
  一意に決まらないときは**同じコードの先頭を採ったうえで `body_unresolved` に控え、`inspect_estimate` / `run_case` が ★ で知らせる**（止まらない）。
  ★ が出た行は**見積書に印字された dm² と必ず突き合わせる**。違っていれば `paint.panels[].index` に見積書の指数を書く

### 5-5. 費用欄の 1〜8 は既定行

コグニの費用画面は 1〜8 が既定行（全角の固定名）、9〜36 が自由記入（半角カナ）。
見積書の費用名は**印字どおり（全角なら全角）**に写す。半角カナで写すと既定行に載らない。
既定行に載るかは語による（漢字・英字のキーワードは載る／カナは ショートパーツ・アライメント・
エーミング・コーティング だけ）。載らなくても金額・合計は同じ。

### 5-5b. 費用に入れるか明細に入れるかは「作業区分が付いているか」で決まる

費用の区画が印字されていない見積で、「リヤナンバー再封印 9,900」を費用に入れるか明細に入れるかは
読み手ごとにぶれる。**金額はどちらでも合う**（工賃計は費用・塗装・内骨を含む集計として突き合わせる）ので
検算では出ないが、コグニで出る画面が変わる。この案件の協定見積書（人が作った NEO の印刷）では、
再封印・脱着・点検の行はすべて**修理方法つきの明細の手入力行**で、費用は区分の付かない
「ショートパーツ 1,000」だけだった。**区分が付いていれば明細、付かず金額だけなら費用**（judgment_rules 10-32）。
手入力行の修理方法は自由文字列（`再封印` `施工` `作業` `再発行` …）で、`DisposalCode -1`・標準欄は空。

### 5-6. その他

- **消費税は 10% 固定**（生成器の `TAX = 0.10`、`Setting.TaxRate=10`）。8% 案件は作れない
- 塗装パネル行は**連動行（明細から起きた行）が部品コード昇順 → パネル追加した行（追加した順）→ 手入力の塗装行（修理方法 9）**の順で保存される（実機 2026-09-20 cogni_pnt_A10 でパネル追加した 0800 が末尾に付いた。実案件 1,800 本でも連動行と追加行が混ざる 76 本すべてで「連動 → 追加」。判断規則 10-20）
- 左右のある部品は**左右別行**にする（コグニ流。数量 2 でまとめない）
- 塗装行の `dm²` は**塗装面積**。20.DB の**パネル面積**とは別物
- 工賃の丸め単位は既定 10 円だが、**100 円で丸める工場**と **1 円単位の工場**（技術料が 10 円の倍数でない。draft が推定）がある（`wage_round`、判断規則 10-6 の 5）

---

## 6. 検証の回し方

```
bash claude_neo_pipeline/tests/verify_all.sh
```

**全部が次の状態でなければ合格ではない**（2026-09-13 時点の基準。件数は NEO_check にある実機 NEO・案件の数で変わるので、分母ではなく「差 0・不合格 0」を見る）:

| 検証 | 合格の基準 |
|---|---|
| 実機 NEO との総当たり | **差のある実験 0 / 73**（この PC、2026-09-13） |
| 全ファイル一致（そのまま保存した実機 NEO） | **25 / 25 本** |
| 案件回帰 | **不合格 0**（NEO_check の案件対応表にある案件回帰 4 件と、スキル側 `scripts/regress_cases.py` の全案件。2026-09-17 現在 19 案件＝ NEO_check に `reading.json` を置いた案件すべて。正解（expected_estimate.json）の無いフォルダは SKIP する（NEO_check にはアプリ・スキルの作業中フォルダも置かれるので、その時点の出力を黙って正解にしない。--update で足す）。不具合を直したら、その案件の `reading.json` を NEO_check に足して `--update` で正解を作る） |
| 単体テスト（生成器・スキル） | `*** FAILED` の行が 1 つも出ない |
| 終了コード | 最終行が **`=== verify_all exit 0`** |

**合格の判定は「最終行が `exit 0`」と「`*** FAILED` が無いこと」で見る。**
個々のテストの合格文言はまちまちで（`all ok` のものもあれば、`cells …` `FBANPA unit …`
`std: …` `known exceptions …` のように固有の数値行を出すものもある）、`all ok` だけを探すと
通っているテストを見落とす。

- 実機 NEO と個人情報を含む検証物は `%USERPROFILE%\Documents\NEO_check\` にある（配布物には入らない）
- 配布先の PC では `python .claude/skills/pdf-to-neo/scripts/env_check.py --self-test`
- **コード変更は Codex レビュー（`codex-loop` スキル、gpt-5.5 / reasoning effort xhigh）を通す**のが標準。
  Codex の指摘は鵜呑みにせず、**実データ（実機 NEO・案件）で裏を取ってから採用**する
  （実際、Codex の指摘のうち何件かは実データで否定されている）。
  **Codex には利用上限があり、使えない期間がある**（2026-09-12 に上限到達、復帰は 09-15）。
  そのときは `claude_neo_pipeline/tests/verify_all.sh` と、次の「文書を機械で見張る」仕組みで代える

### 文書が古くなるのを機械で見張る

`claude_neo_pipeline/tests/unit_handoff.py`（`claude_neo_pipeline/tests/verify_all.sh` に組み込み済み）が、**この HANDOFF.md に書かれた主張を実装・実データと突き合わせる**:

- 参照している文書・スクリプトが実在するか
- §5 の「生成器はこうしている」が実際のコードと合っているか（関数名・条件式で照合）
- §5-1 の数値（実機 NEO 31 本 / パネル 103 行 / 連動 80 行 / 追加 23 行）が実機と合っているか。
  **「連動なのに明細に無い行」が実機に 1 件でも出たら不合格**にする（規則そのものの崩れを検知）
- §5-4 の枝番・同一コードの件数が ADDATA の実データと合っているか

**HANDOFF.md を直したらこのテストも直す**。逆に、このテストで確かめられない主張は書かないほうがよい。

---

## 7. これまでに踏んだ落とし穴

| 症状 | 原因 | 教訓 |
|---|---|---|
| 塗装計が実機で 33,950 不足 | 枝番パネルを連動（`AddedFrom=0`）にした | **ファイルの中身が合っていても実機で開くまで分からない**。§5-1 |
| 全ファイル一致が 25 → 17 本に落ちた | 未定義変数を `except Exception: pass` が握り潰していた | 例外を黙って捨てない。理由を `silent_errors` に残して報告に出す |
| 失敗の理由を控えたのに誰も読んでいなかった | 控え用の属性を作っただけで、報告に載せていなかった | 控えるだけでは握り潰しと同じ |
| 車種をまたいで塗装指数がずれた | `PaintIndex` のインスタンスを使い回した | 車種・ボディをキーにキャッシュする |
| 費用が既定行に載らなかった | 費用名を半角カナで写した | 見積書の印字どおりに写す。§5-5 |
| 塗装工賃計が 9,700 円不足 | ボデーシーリングが `paint.other` に落ちていた | 専用項目が other に落ちていないか `_draft_notes` で見る |
| 文字列 `"false"` が真になっていた | `if d.get('generic'):` で真偽値を読んでいた | 人が書く JSON の真偽値は**入口で 1 回正規化**する（`_flag` / `skill_env.flag`）。箇所ごとに直すと必ず取りこぼす |
| 不合格の NEO が納品物の名前で残った | 検算より前にファイルを確定していた | 検算に通ってから納品先に置く（不合格は `.ng.neo`） |
| 部品計のずれが見逃された | 合計用の許容差を全項目に効かせていた | 丸めの出る項目だけに限る。部品計・材料代・費用は 0 円一致 |

---

## 7-2. 文書を信じる前に

この一式の文書は**実装より古くなっていることがある**。2026-09-12 の監査では、実装と突き合わせて
21 件の食い違いが見つかった（AnSMB の桁の意味、20.DB のバイト表、一括塗装時の設定値、
スクリプトの案内するキー名など）。**バイト位置・列の意味・キー名は、必ず実装で確かめてから使う**。

「正の実装」の対応表は `ADDATA_REVERSE_LOOKUP_SPEC.md` の LCG の節にある。とくに:

- `_addata_db_search.py` は**現行が実行時に使っている**（`LCG` / `_read_seed` / `load_11db` /
  `load_12db` / `load_15db`）。消すと動かない。ただし**バイト位置の仕様としては
  `claude_neo_pipeline/` 側が正**（`load_83db` / `get_all_parts` などの検索系は旧アプリ用で、
  スライス位置が仕様書と違う）
- 文書どうしが食い違ったら `reference/judgment_rules.md` が正本

文書を直したら、**実装のどこを読んで確かめたか**を書き添える（日付も）。次に読む人が再検証できる。

---

## 8. まだ確かめられていないこと

- ~~W90 の実機 NEO が手元に無い~~ → **2026-09-12 に人が最初から手入力して取得した**
  （実機 NEO は `NEO_check/_eva_exp/cogni_W90.neo`。記録は `reference/experiments/2026-09-12_W90_実機手入力.md`）。
  §5-1 の規則はこれで直接確認済み。この実験で生成器の誤り 5 件（架空の標準指数 8.1、
  連動部品の他ボディ行、標準なし行の WorkCode、パネル追加時の加算基礎の印、4WD の CarEVA）を直した
- ~~標準の無い取替行の `ChangeTotal`~~ → **2026-09-12 に再現**: ボディ専用行（sub が枝番違い 4801）が共通行を押しのけた部品は
  Time -1 のまま、取替合計にはそのボディ行の指数（W90 4800 = J3 9.0 + Q4 0.7 = 9.7h）が入る。`_std_pick15` の host_fallback で枝番違いの行を採る
- **W90 4600 + 4800 の連動加算 +7.7h**（`cogni_W90b`）: 4600 単独 4.0（N 0.4 + O 2.0 + P 1.6）が 4800 を足すと 11.7 になる。
  15.DB の 4600 には K 行（link MM・sub 5001・ボディ 20）に 1170 = 11.7 がそのまま載る（J 行 sub 4801 は 9.0）。
  「左クオータ 4800 を足したのに右クオータ 5001 の行の値」なのか「9.0 − 1.3 の差し引き」なのかは 1 例では決められない。**未解決で、金額に影響する唯一の項目**。
  4600 取替 と クオータパネル取替 が同時の案件は `run_case.py` が ★ で知らせるので、コグニ実機で 4600 の指数を確認して違えば index を `#` で書く（既知差 W90b で管理）
- ~~下処理面積 `PrepareArea` の W66 ルーフ~~ → **2026-09-21 に式を確定**: `min(切上(塗装面積 ÷ 3), HJU_SS の上限)` で、
  この列は「HJU_SS を引く index」そのものだった（実案件 1,097 行。2 式が割れた 188 行で旧式が正しい行は 0）。W66 ルーフも合う（painting.md §17-1）
- ~~25.DB の読み方~~ → **2026-09-21 に訂正**: 13B レコード（ヘッダなし）。ボディで車形が変わる車種が 227/1,209 あり、
  実案件の Car テーブルとの一致が 973/994 → 993/994。車形は加算基礎・ブース・バンパ・高機能・溶接パネル下処理のキーなので金額に効く
- ~~塗り数値の CHM~~ → **2026-09-21 に訂正**: 76.DB はボディごとに別の CHM を指している（88 車種）。
  ファイル名の取り出しが誤っていて常にボディ 00 用に落ちていた（実案件 600 本で塗装パネル 98.2% → 98.7%）
- ~~複数品番候補から選んだ行の印~~ → **2026-09-21 に確定**（実案件 900 本・明細 47,717 行）: `OrderFlag` は
  `''` 触っていない / `'0'` ADDATA の品番のまま / `'9'` 品番か価格を人が入れ替えた / `'1'`〜`'3'` **複数候補から選んだ番号**（品番末尾が必ず `*`）/ `'*'` 選んだ直後。
  金額には影響しない（NEO 仕様書 §10-17 の表）
- ~~パネル無しでバンパだけ塗装（`BumperBase*`）~~ → **2026-09-12 に実機で確認・実装**: `panels: []` + `bumper_front` で書く。
  加算基礎数値は -1、BAN.DB のバンパ加算基礎が `BumperBase*` に入りバンパ計に含まれる（`cogni_W66w` 全列一致）
- ~~バンパ塗装の標準指数~~ → **2026-09-21 に実案件 145 行で 145/145 一致**（それまでは 98 行）。2 つ直した（`reference/painting.md` §15）:
  ① 表の無い車の `COM/FBANPA.DB` は 6 値が 3 列ずつ 2 群で、**塗装パネルがあれば v1..v3・バンパだけなら v4..v6**
  （実機 J69 の実験はパネル 0 枚だったので v4 群。これを全車に当てていた）。
  ② `<car>23.DB` / `93.DB` は **年式群・ボディ・グレード・装備でも行が分かれる**（1,430 ファイル中 316）。
  パネル表と同じ規則で絞る。グレードが分からない案件は見積書の指数を `paint.bumper_*.index` に書くこと
- ~~CHM に 1/2・1/3 の無いパネル~~ → **2026-09-12 実機**: ロッカパネルアウタ 2602 は修理でも 1/1 固定（`cogni_W66z`）。生成器は 1/2 指定でも 1/1 で書く
- ~~ADAS のセンサ別作業の基本作業~~ → **方針を決めた（2026-09-12）**: 生成器は見積書に書かれた作業だけを書き（工場のコグニが出した見積書には基本作業も印字されている）、
  センサ別作業の前提作業（56.DB `BasePartsCodes`）が `adas` に無ければ run_case が ★ で知らせる。自動では足さない
- **レバーレートの仮定 7,280 円**（2026-09-12）: 見積に `labor_rate` が無く工賃÷指数からも決められないときだけ。`stats.labor_rate_assumed` を立て run_case が ★ で知らせる
- ~~材料代割合の既定値は PC のコグニ設定~~ → **2026-09-12 に解決**: 実案件 NEO 4,933 本の 83% が SHOUCHIKU 見積ガイドラインの
  「塗装材料代割合」表の **6500〜 列**（工賃単価によらない）だった。見積に材料代も割合も無いときの既定をこの列にした
  （表は社内の参考値なので git に入れず `<NEO_CHECK_ROOT>/_reference/shouchiku_guideline.json`。無い PC はコグニ既定。`env_check.py` が有無を表示）

### 8-2. 実案件 NEO 7,002 本（`Z:\ドキュメント`、2025-12〜2026-09）で分かったこと（2026-09-12、`tests/corpus_scan.py`）

亮平さんがコグニで作った NEO を正解にして、生成器を読み取り専用で突き合わせた（顧客情報は書き出していない）。

| 再現率（この PC の ADDATA と同じ 2026/08 版の 638 本） | 結果 |
|---|---|
| 車両特定 | 560/638（87.8%）。外れの大半（65 本）は NEO に型式指定が無い＝入力不足 |
| 部品照合（品番+名称+価格 / 名称+価格 / 名称のみ） | 98.2% / 96.4% / 94.7%（37,037 行） |
| 標準指数 | 95.9%（3,822 行） |
| 塗装パネル指数 / 加算基礎 | **99.2% / 100%**（2026-09-21 に測り直し。87/97.DB を使うようにしたのと、測定ツールが手入力の塗装行・高機能だけの行まで枚数に数えていたのを直した。250 本・パネル 509 行・加算基礎 164 本） |

直したもの:
- **COM の表を同梱の古い写しから読んでいた**: `DATAUP.DB`（→ `WorkCodeUpdateDate`）と `Katashiki.DB`（型式 → 車種）は ADDATA の月次更新で変わる。
  使っている ADDATA の `COM\COM.CAB` を版ごとに展開して読むようにした（`com_tables.py`。車両の一致が 544 → 560 本）
- **高機能塗装「スクラッチ」**（`HFPainting 3`、名称 `ｽｸﾗｯﾁ`。100 本）を扱えなかった（知らない名前は黙って「しない」になっていた）。
  加算基礎・ブースは T_KEI_3 / BOOTH の S 列で出す。知らない名前は止める
- 材料代割合の既定（上）
- **塗装パネルの修理方法 9 = 部品コードの無い手入力の塗装行**（631 行。外板パネル画面の「行追加」）。2026-09-13 に対応:
  `paint.panels[]` に `{"manual": true, "name", "index" か "wage"}` で書く（材料代の対象・加算基礎の枚数には数えない）。
  下書きは 20.DB に無い部位名をこれに、工程名（アンダーコート・内板調色 …）を追加項目にする（判断規則 10-20）

まだ対応していないもの（重要度順）:
- ~~塗装パネルの修理方法 7 = 高機能塗装だけのパネル~~ → **2026-09-20 に実装**（`paint.panels[]` の `method: "高機能"`。painting.md §13(2)）
- ~~スクラッチのパネル加算の式~~ → **2026-09-21 に解読・実装**（`Scrach.DB` の A/B と `87.DB` の高機能列）。実案件 82 パネルで 82/82 一致
- ~~標準指数の外れ 4%~~ → **2026-09-21 に規則を特定して実装**（実案件 600 本で 95.9% → **98.7%**、638 本の測定で
  直った 106 行・壊れた 0 行。既存テストの結果は 1 つも変わらず）。原因は骨格の集約ではなく、次の 4 つだった:
  - **R1 偶奇リンクの抑止を「自分の行どうし」にも効かせる**（いちばん大きい要因。−65 行）。
    15.DB の行の link 符号で、奇数 X(2k+1) の行は 偶数 X(2k) の行が有効なら指数に入らない。
    それまでは「同ブロックの他部品の区分」と外部リンクでしか見ていなかった
  - **R2 相手と共有する行**（−33 行）。host が自分・sub が相手の行は、相手が見積に居て、その区分が
    相手自身の区分でもあるとき自分の指数にも入る（両者が同じ 1 行を見る）。
    実案件 J97 4600 Rrパネル取替 12.1h = K3 370 + **L3 760（sub 4800・4800 も明細に居る）** + P3 80
  - **R3 連動 1 の相手に分解調整(5) を含める**（−6 行）
  - **R4 行の選び方**: ボディ固有行をグレード・装備の一致より優先し、一致した装備文字の数が多い行を優先
  - 以前の「自分のリンク符号が同ブロックの他部品の区分と一致 → 吸収」は外した（R1 と重なって消しすぎる）。
    `_active_links` による外部リンクの抑止は残す
  - **残り 48 行**（1.3%）: 17 行は骨格ブロック（`_frame_combination` は今回触っていない）、
    11 行は X25 の「相手の 0.9〜1.3 がどの明細にも現れず消える」型（条件が見つからない）
  - **骨格ブロックの行選びは意図して従来のまま**にした。非骨格と同じ順位（ボディ固有優先・装備文字数優先）に
    揃えると実案件 638 本で 797 回も選ぶ行が変わるのに、**出力は 1 行も変わらない**（変わった行はすべて
    骨格以外の部品に集約されるため表に出ない）。骨格の順位を決めたときの根拠（実機の画面読取 23 通りと
    保存版 FRAME_p7/p8/p9）は従来の並びで取ってあり、新しい順位の根拠は非骨格の例しか無い。
    変えるなら先に「装備やボディで割れる骨格部品」を実機で 1 件作って確かめること（パッチは作ってある）
- ~~15.DB の link の 2 文字目が英字（同字 `AA`/`II`/`JJ`）の行~~ → **2026-09-21 に解決**:
  重ね文字のリンクは **偶数 `X0` と同じように 奇数 `X1` の行を抑止する**（`_link_even_form`）。
  実案件 638 本で 98.73% → 98.92%（直り 7 行・壊れ 0 行）。
  なお「同系列のまとめ行」としての合算式（`II` = 同系列の `I0` 行の合計か）は別の話で、まだ確定していない
- ~~W90 ハイエースの +7.7h~~ → **2026-09-21 に原因を特定**（金額に効く既知差はこれで無くなった）。
  実機の 11.70 は `N3 0.4 + P3 1.6 + J3(sub 4801) 9.0 + Q4(sub 4801) 0.7` で、**`J3` のリンク `LL` が
  `O3` のリンク `L1` を抑止**して `O3 2.0` が落ちる構成。上の重ね文字リンクの規則で半分が説明でき、
  残りは「R2 の共有行の sub が**ボディ別の枝番**（4801）のとき、明細にある元コード（4800）を相手とみなす」で再現する。
  **後者は実装していない**: 実案件 7,216 本に W90 でこの組合せは 0 件、同じ型の取りこぼしも 2 行（各 +1.3h）だけで、
  緩和を `_std_pick15` まで広げると別の 2 行が壊れる（実測）。実機でもう 1 パターン作ってから入れるのが安全
- **4600（Rrパネル）取替の残り**: 実案件 434 行のうち外れ 54 行。うち 52 行は「クオータ取替が同時にあると
  生成器が高すぎる」型で、+7.7h とは逆向き。連動加算・吸収の効かせすぎで、15.DB のリンク構成でもう一段層別が要る
- ~~耐スリ傷のパネル加算~~ → **2026-09-21**: 車種の表（`87/97.DB` → CHM）を先に見るようにして、高機能塗装のパネル加算は実案件 825 行で **821 行（99%）**が一致
- ~~`ERParts.OrderFlag '9'` の意味~~ → **2026-09-21 に確定**: 品番か価格を人が入れ替えた行（汎用車種の手入力行を含む）
- ~~`Total.wg_IncludeMaterial`~~ → **2026-09-21**: 実案件 900 本で割増・値引きの金額が入った NEO は **0 件**。値引きが無ければどちらでも金額は同じ
- **測定ツールが生成器と違う条件で測っていた**（2026-09-21 に修正）: `tests/roundtrip.py` の `paint_test()` が
  `PaintIndex` にボディも装備も渡していなかったため、条件行のある表を生成器と別の行で引いていた。
  直したら実案件 600 本の塗装パネルが **98.7% → 99.5%**（実態はもっと良かった）。
  **検査ハーネスは生成器と同じ入口（`PaintIndex(..., body=) + set_vehicle()`）で作ること**
- **この PC の ADDATA は 2026/08 版だが、2026/09 版で作られた NEO が 157 本ある**（別の PC は更新済み）。この PC も ADDATA を更新する
- レバーレートは特定の 2 つの値に 3 割が集中している。ガイドラインの単価表の値と一致するのは 25%（単価は従来どおり見積書から逆算）
- ~~`target_total` / `fit_paint_total` のガード~~ → **2026-09-12 に実装**: 工場見積に材料代の内訳が印字されていれば
  `auto_panels` は一括計上に戻し、`target_total` は `target_total_replaces_material: true` が無いと止まる
- ~~修理(2) の塗装パネルが自動連動する条件~~ → **2026-09-12 に実機で確認**: 修理(2) を明細に入れただけで
  20.DB にあるパネルは **1/2** で自動連動する（W66 5 枚すべて。`cogni_W66x` / `cogni_W66y`、判断規則 10-18）。
  下処理面積 `PrepareArea` は近似式が 4 枚で合い、W66 のルーフだけ合わない（表示のみ・既知差 W66y）

---

## 9. 環境

| もの | 場所・備考 |
|---|---|
| ADDATA（コグニのマスタ） | `C:\Addata`（`COM` とメーカー別フォルダ `W`,`J`,`D`… があるフォルダ）。**NEO 生成に必須** |
| コグニセブン本体 | `C:\Program Files (x86)\Audatex\Auda7\Bin\AudaMenu.exe`。実機確認のときだけ必要 |
| Python | 3.11 以上（3.14 まで確認済み）。追加パッケージ不要 |
| `hh.exe` | Windows 標準。塗装指数の CHM 展開に使う |
| 案件フォルダ | `%USERPROFILE%\Documents\NEO_check\<案件名>\`（環境変数 `NEO_CHECK_ROOT` で変更可） |
| 設定 | `%USERPROFILE%\.claude\pdf-to-neo.local.json`（`env_check.py --save` が作る） |
| git のブランチ | 作業と GitHub 送信は **`pdf-to-neo`**。GitHub のリポジトリは**公開**なので、顧客情報を含む版を一度でも commit したらその途中版は送らない（まとめてから送る）。この PC の `v6-clean-local-only` は途中版に顧客情報を含む記録用で、**push しない**（`.git/hooks/pre-push` が止める）。GitHub の `v6-clean` / `main` は旧 Streamlit アプリの系統なので触らない |

新しい PC での初期設定は配布 zip の `BUNDLE_README.md` を見る。

**実機コグニを操作するときの注意**: IME の入力ウィンドウ（`TextInputHost`）が前面判定を奪って
操作できなくなることがある。そのときは `TextInputHost` を終了させてから、
`SetForegroundWindow` で目的の窓を前面にする（Windows が自動で再起動する軽量プロセス）。


---

## 未解析・未確認の棚卸し（2026-09-21。Codex に全文書を読ませて洗い出させたもの）

金額に効く順。**「確かめ方」は実機操作をなるべく使わない方法**を先に書いてある。

### 金額に直接効く（先に片付ける）

| # | 穴 | 確かめ方 |
|---|---|---|
| 1 | **標準指数の外れ 4%** | 規則は特定済み（偶奇リンクの抑止範囲と sub 付き行の共有。実案件 638 本で 95.9% → 98.8%）。実装して回帰を通す |
| 2 | **W90 ハイエースの +7.7h**（既知差。合計 48.2h に対し生成 40.5h） | W90 系だけを全 NEO から抜き、作業区分・部位・左右・塗装の有無でまとめて、+7.7h が特定の条件か 1 件の例外かを見る |
| 3 | **レバーレートの仮定 7,280 円** | 工場・保険会社・作成年月ごとに実案件の税抜レートを集計し、「単価の印字が無い案件」だけで誤差を測る |
| 4 | **ADDATA の版違いが統計に混ざっている**（実案件の 157 本は 2026/09 版で作られているが、この PC の COM は 2026/08） | 作成年月と COM の版を揃えた部分集合だけで、車両特定・部品価格・指数・塗装を測り直す |
| 5 | **材料代の割合と丸め**（既定は社内ガイドラインの 6500〜 列。366 件中 5 件が説明できず、22 件は切上のみ） | 工場・保険会社・塗装種別ごとに割合と丸めを推定し、材料代の印字が無い案件だけで誤差を測る |
| 6 | **13.DB の複数候補**（実機は候補ダイアログを出して人が選ぶ。生成器は期間内の先頭。8/102 件が不一致） | 不一致 8 件を グレード・型式・トリム・部位名・候補どうしの価格差で比べ、選択の特徴を探す |
| 7 | **20.DB の面積が割れるパネル**（同条件で面積の違う行が 258 組。先頭を採って警告） | 割れる組だけを母集団にして、実案件の面積・指数・CHM のキーと突き合わせる |

### 仕様・機能の未確認（実務で出たら効く）

| # | 穴 | 確かめ方 |
|---|---|---|
| 8 | ADAS の基本作業の自動追加（24.DB 連携は推定のまま） | ADAS 部品を含む実案件で、取替と同時に入っている作業コードの頻度を 24.DB の候補と突き合わせる |
| 9 | 89.DB（作業項目確認）の採用規則 | 部品の取替と同時に出る工賃コードを実案件から掘り、89.DB の候補の採用率を部品別に測る |
| 10 | トリムコードが部品の候補に効くか | 同じ型式でトリムだけ違う車を抜き、同じ部位の品番・価格差を 13.DB と実案件で比べる |
| 11 | 値引き・割増・総額調整（900 本で 0 件） | 総額と明細合計が合わない NEO を逆に抜き、どの列で吸収しているかを見る |
| 12 | W/S ワークシート版の切り替え条件（装備 T でも期待どおりに変わらない例がある） | 版によって変わる候補部品・指数の差を、装備・型式・グレード別に比べる |
| 13 | 重複部品の親子（46.DB。3 車種 11 組の画面観察が根拠） | 同じ部品コード・別名・左右違いの実案件を広く集めて、候補と実際に選ばれた行の関係を数える |
| 14 | コグニ操作で工賃が消える 7 行の条件 | その 7 行の部品種別・作業コード・指数・手入力の別を横に並べ、同条件の別 NEO で消えるかを見る |
| 15 | 品番 `-` で工賃だけある行の意味 | その行を全部抜き、作業コード・名称・金額と見積書の表記を突き合わせる |
| 16 | `AnSMB.txt` の `[12]` `[103]` `[105:116]`（NEO 仕様書に追記済み） | 実機で 1 件ずつ作って保存し、桁の変化を見る |
| 17 | S_Est の 1 桁目（10/20/50/60 の算出方式）、`T_KEI_4`、`SIRU.Time2`、`BAN.DB` の 4・5 列目 | その値を持つ実案件・ADDATA 行だけを抜き、作成年月・車種・指数差で実使用を判定する |
| 18 | バンパの暫定 `$` 印（1 例からの推定） | `$` を含む実案件と見積書を集め、部品価格・標準指数・部位が通常と違うかを比べる |
| 19 | 車両特定 87.8%（外れると部品価格・指数・塗装が全部ずれる） | 外れた車だけを 型式・年式・グレード・輸入/国産・型式の印字なしで層別し、金額差の大きさを出す |
| 20 | 部品照合の内訳（全体率が「名称だけで照合した案件」の危うさを隠している） | 品番あり / 品番なし / 名称だけ / 左右の語あり で分けて精度と金額の誤差を測る |

### 検証の作り方そのものの弱さ

- 機能ごとの実験 NEO が **1 機能 1 例**に近い（実験 73 本・全ファイル一致 25 本・回帰 20 件）。
  メーカー・年式・ADDATA 版を散らした最低限の本数を決めて測り直す
- 全体の一致率で語っている規則がある。**その規則が実際に効く行だけを母集団にして**測り直す
  （2026-09-21 のバンパはこの方法で決着した）


---

## コグニの機能で生成器が未対応のもの（2026-09-21。実案件 1,000 本の全列走査）

**金額に効く未対応は実質ゼロ**（値引きの率指定・丸め設定だけで、使っている実案件は 1,000 本中 1 本）。
残りは帳票・画面の見た目。着手するならこの順。

| # | 列 | 出現 | 金額 | 内容 |
|---|---|---|---|---|
| 1 | `ReportTitle.ReportTitle` | **約 20%** | なし | 帳票の表題（`協定見積書` 108 / `概算見積書` 83 / `認定見積書` 20 / `全損見積書` 13）。生成器は雛形の「お 見 積 書」のまま |
| 2 | `Expense` の自由行（9〜36）の行番号 | — | なし | `KEYMAP` に無い費用名が行 36 から降順に置かれ、**工場の定型行名を上書きする**。未収録: `ｷｬﾘﾌﾞﾚｰｼｮﾝ`（実機は行 29）・`電子機器再設定/点検清掃/診断点検`・`ﾒﾓﾘｰﾘｾｯﾄ`（半角カナ）・`ｼｰﾘﾝｸﾞ`・`ﾄﾞﾗｲﾌﾞﾚｺｰﾀﾞｰ脱着`（行 16）・`ETCｱﾝﾃﾅ脱着`（行 23） |
| 3 | `DamageBlockPlan.DamageCode / FrontArea / RearArea / AllArea` | 一致 51.7% | なし | 損害報告書の部位図の範囲。生成器は常に全部位。実案件は 前部のみ 14.8%・後部のみ 10.5% |
| 4 | `ERParts.DisposalName` の自由文字（区分コードは 0〜6 のまま） | 約 300 行 | なし | `(1,'脱着取替')`・`(1,'一部脱着')` など。生成器は未知の作業名を必ず手入力行（-1）にするので、コード付きのまま自由名を印字できない |
| 5 | `ERParts.DisposalName='部品'`（区分 -1） | 925 行 | なし | 生成器は `method='部品'` を取替（0）＋名称「取替」にする |
| 6 | 値引きの率指定 `pt_ExtraRate / wg_ExtraRate`・丸め単位・`IncludeRecycle/IncludeMaterial` | 1/1,000 | **あり** | 生成器は金額指定のみ |
| 7 | `PaintingFrame` の選択肢ラベル（`修正`・`片側`・`両側新品`） | 1〜8 本 | なし | 指数と工賃は書けているがラベルが既定のまま |
| 8 | `Ex.db [PaintingPlan] FormShow=0` | 16.1% | なし | 塗装画面の表示フラグ。`0` の本は塗装パネルが 0 行だが、**逆は成り立たない**（パネル 0 行でも 1 が 68 本） |

**実案件でも使われていないと確定した機能**（対応不要）: リサイクル置換（`RCParts`/`RCLinkParts` 0 行）、
`EPCLinkParts`、損傷コメント・損傷画像・画像注釈、`Unspecified`、レッカー（`hy_Wrecker` 全件 0）、
工賃単価の区分別設定（全件 0）、協定額（`TimelyPrice` 全件 -1・`AgreedDate` 全件空）、
**複数見積を 1 ファイルに持つ仕組み**（`AnSvEm0002` は 0 件）。

部品の「支給・持込・中古・リビルト・社外品」には**専用のフラグが無い**（品番か名称の自由文字＋価格の印 `*` で区別。
1,000 本で `社外` 38・`参考価格` 35 のみ）。生成器は品番・名称をそのまま書けるので実質対応済み。


---

## 棚卸し 30 項目の決着（2026-09-21。エージェント 7 本 × Codex で全件あたった）

### 直したもの

| 項目 | 結果 |
|---|---|
| 標準指数の外れ 4% | **98.9%** に（重ね文字リンク・R1〜R4） |
| W90 の +7.7h | **正体は重ね文字リンク**。解決 |
| 材料代の丸め | **1 円切り上げ → 10 円四捨五入**。実案件 92.5% → 99.98% |
| 20.DB の面積が割れるパネル | **ボディ以下で最大**の行を採る（枝番が先）。塗装パネル 99.5% |
| AnSMB の CutWork | 英字の無い欄も写す。46,437 行で 99.38% → 99.99% |
| 塗り数値の CHM・車形（25.DB）・色別の塗装条件（66/96.DB） | すべて読み方を訂正 |
| **テスト 5 本が落ちない** | 終了コードを付けた（検証の土台） |

### 「やらなくていい」と決着したもの（調べた上での結論）

| 項目 | 理由 |
|---|---|
| **89.DB の関連作業の自動追加** | 埋まる行は無く金額は 1 円も動かない。平均 4.3 行/本を誤って足すだけ。本体も `AutoMakerCodes=I` でメーカー I しか自動計上しない。★ の確認リスト用途なら採用率 80% 超の部品（4315/6000/6100/2700/0600/2300）に限って価値あり |
| **ADAS の 24.DB 連携** | ADAS 作業は実案件 7,216 本中 2 本（`ReserveERParts`）。実務では費用欄に書かれる（`ｴｰﾐﾝｸﾞ費用` ほか 2,240 件）。24.DB は ADAS の表ではなく Assy 構成 |
| **KATB120 による板金の上限** | KATB120 は部位図とブロックの対応表で上限は入っていない。上限は「取替指数＋部品代÷単価」の経済的上限で、超過は 699 行中 9 行（1.3%・いずれも部品価格が無い行） |
| **15.DB の重ね文字リンクの合算式** | 式は無い。まとめ行の値は ADDATA の実測値（左右同時作業の重複を引いたもの）。同ホスト内では書けない（65.8% は仲間が別ホスト） |
| **SIRU.DB で低隠蔽性** | SIRU は低隠蔽性ではなく**ロッカパネル／サイドシルの塗り数値**。低隠蔽性は今の `fukaetc` が正しい（157/157） |
| **材料代の単価方式** | 実案件 7,215 本すべて `MaterialUnitFlag=0`（率方式）。使っている工場が無い |
| **DamageBlockPlan を明細から決める** | 明細から判定すると 56.2% で、常に ALL（64.4%）より悪い。ALL は「全部見えるように押す」人の癖 |
| **値引き・割増の率指定** | 金額が入っているのは 7,215 本中 5 本。先に直すべきは**保留行を計から除く**（37 本） |
| **46.DB の親子** | **46.DB は存在しない**（書き誤り）。親子は 12.DB のレベル欄。同居しても 7,077/7,086 は両方そのまま載る（今の重複警告はほぼノイズ） |

### まだ残っているもの

| 項目 | 次の一手 |
|---|---|
| **レバーレートの既定 7,280 円** | 期間別にすると工賃計のずれが中央値で 21% 減る（〜2026/06 は 7,280・2026/07〜 は 7,820）。**工場別の対応表**（ローカル）を持てば ★ は実質消える（4,688/4,958 工場は単価が 1 種類） |
| **部品の価格が ADDATA と合わない** | 版違いが原因。**2026/04 と 03 の間に価格改定**があり、−5 か月以上ずれると一致率が 30% 台に落ちる。手元の版と揃えた部分集合では 98.2% |
| **13.DB の複数候補** | 規則は無い（実機も人が選ぶ）。ただし「11.DB 変種と同じ品番を優先」で 72.6% → **77.6%** に上がる |
| **車両特定 87.6%** | 内訳は「型式なし 551 本（入力不足）」「グレード・年式の絞り込み不足 275 本」「本当に別の車 10 本（0.25%）」。確度 medium（372 本・46.8%）だけ人に確認させれば外しの 72% を捕まえられる |
| **品番の無い見積** | 品番が 1 つも印字されない案件（FAX 概算）は **7 割が部品計 5% 以上ずれる**。`stats` に品番印字率を出して ★ を立てるべき |
| **実機実験の偏り** | 89 本が 14 車種・6 メーカー・ADDATA 2 版に集中。**日産・マツダ・スバルはゼロ**（実案件の 14.7%）。実案件の 82.4% は実験のない車種 |
| `Com\AdVer` の版分岐 | 本体は 01/11/13/26/66/83/96.DB のレイアウトを版で切り替える。生成器は未対応 |
| ReportTitle | PDF からは決められない（案件の段階を人が選ぶ）。`estimate.json` に入力項目を足すのが正しい |
| Expense の行番号 | 行 9〜36 の名前は**雛形（工場のコグニ設定）で決まる**。生成器の雛形の行名と正規化一致で決めるのが正しい（現行の KEYMAP は部分一致で 6 か所ずれる） |
